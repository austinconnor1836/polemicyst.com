app_identifier(ENV["APP_IDENTIFIER"] || "com.polemicyst.app")
apple_id(ENV["APPLE_ID"])
team_id(ENV["APPLE_TEAM_ID"])

for_platform :ios do
  for_lane :beta do
    app_identifier("com.polemicyst.app")
  end
  for_lane :release do
    app_identifier("com.polemicyst.app")
  end
end
